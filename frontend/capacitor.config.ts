import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saikiran.placement',
  appName: 'Placement Intelligence',
  webDir: 'build'
};

export default config;
